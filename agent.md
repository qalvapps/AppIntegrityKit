# AppIntegrityKit Agent Guide

The authoritative agent instructions are in [`AGENTS.md`](./AGENTS.md).

AppIntegrityKit is an end-to-end security product with a Swift client, Python
server-verifier package, versioned wire protocol, deterministic cross-language
test vectors, DocC documentation, and XCFramework packaging support.

Public Swift entry point: `AppIntegrity.shared`.

